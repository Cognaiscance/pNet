# pnet_installer

Phase-2 installer agent (desire + status, **no auto-install**).
See [description.md](description.md).

```bash
PNET_AUTO_APPROVE_APPS=1 cargo run -p pnet
cargo run -p pnet_installer
```

Open the portal, sign in, Home → **Installer** (`/apps/installer/`).
