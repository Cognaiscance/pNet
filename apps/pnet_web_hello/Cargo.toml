[package]
name = "pnet_web_hello"
version = "0.1.0"
edition = "2021"
description = "Sample pNet hybrid app: loopback HTTP UI + portal mount registration"

[[bin]]
name = "pnet_web_hello"
path = "src/main.rs"

[dependencies]
ctrlc = { version = "3", features = ["termination"] }
